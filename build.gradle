plugins {
    id 'groovy'
    id 'java-gradle-plugin'
}

group = 'xyz.venuprasath'
version = '0.0.1-SNAPSHOT'

repositories {
    jcenter()
}

dependencies {
    testImplementation('org.spockframework:spock-core:1.3-groovy-2.5') {
        exclude module: 'groovy-all'
    }
}
